#include <stdio.h>

int main() {
    int i=0;
    do{
        printf("The value of i is %d\n",i);
        i++;
    }
    while(0);  // do-while = while loop which executes at least once
               // while checks the condition
    return 0;
}